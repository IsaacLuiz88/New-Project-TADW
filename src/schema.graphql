type Query {
    info: String!
    feed: [Link!]!
    users: [User!]!
}

type Link {
    id: ID!
    description: String!
    url: String!
}

type Mutation {
    createUser(name: String!): User!
}

type User {
    id: ID!
    name: String
}